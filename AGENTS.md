# Agent Guidelines

- Use `pnpm` instead of `npm` for all package management commands
- When making changes, always run `pnpm test` to verify tests pass
- When making changes, always maintain 100% code coverage
